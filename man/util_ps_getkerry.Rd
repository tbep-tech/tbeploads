% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_ps_getkerry.R
\name{util_ps_getkerry}
\alias{util_ps_getkerry}
\title{Get Kerry I&F discharge monitoring report data from FDEP OCULUS}
\usage{
util_ps_getkerry(
  yr,
  search_xlsx,
  pdf_dir = NULL,
  out_file = NULL,
  quiet = FALSE
)
}
\arguments{
\item{yr}{numeric (length 1), the monitoring year to retrieve (e.g., 2025).}

\item{search_xlsx}{character, path to an OCULUS search-results spreadsheet
for facility FL0037389.  See Details for instructions on generating this
file.}

\item{pdf_dir}{character or NULL.  Directory in which to save the downloaded
PDFs.  If \code{NULL} (default), a temporary directory is used and all PDFs
are deleted when the function exits.  If a path is supplied, PDFs are
retained there.}

\item{out_file}{character or NULL.  If provided, the results data frame is
written to this path as an \code{.xlsx} workbook.}

\item{quiet}{logical.  Suppress progress messages (default \code{FALSE}).}
}
\value{
A data frame with one row per available monitoring month, sorted by
month.  Calendar months for which no Part A document was found are
omitted (a message is printed when \code{quiet = FALSE}).  Columns:\tabular{lll}{
   Column \tab Type \tab Description \cr
   \code{yr} \tab integer \tab Monitoring year (from the PDF monitoring period). \cr
   \code{mo} \tab integer \tab Calendar month (1-12, from the PDF monitoring period). \cr
   \code{outfall} \tab character \tab Always \code{"FLW-3"} (the combined flow). \cr
   \code{flow_mgd} \tab numeric \tab Average Daily Flow (MGD), monthly average. \code{0} indicates no discharge (NOD). \cr
   \code{bod_mgl} \tab numeric \tab BOD, Carbonaceous (mg/L), monthly maximum. \code{NA} when ANC or not reported. \cr
   \code{tss_mgl} \tab numeric \tab Always \code{NA} (not monitored at this facility). \cr
   \code{tn_mgl} \tab numeric \tab Total nitrogen grab-sample concentration (mg/L). \code{NA} when ANC or not reported. \cr
   \code{tp_mgl} \tab numeric \tab Total phosphorus grab-sample concentration (mg/L). \code{NA} when ANC or not reported. \cr
}
}
\description{
Downloads and parses monthly Part A Discharge Monitoring Report (DMR) PDFs
for the Kerry I&F Contracting Company industrial wastewater facility
(NPDES permit FL0037389, Hillsborough County, FL) from the Florida
Department of Environmental Protection (FDEP) OCULUS public document
management system.  Returns Average Daily Flow, BOD, Total Nitrogen, and
Total Phosphorus concentration for each available monitoring month.
}
\details{
\subsection{Generating the OCULUS search spreadsheet}{

The \code{search_xlsx} file is an Excel export from the FDEP OCULUS public
document portal.  To generate it:
\enumerate{
\item Navigate to \url{https://depedms.dep.state.fl.us} in a web browser.
\item Click \strong{Public Oculus Login} (no account required).
\item In the search form, set:
\itemize{
\item \strong{Catalog}: Wastewater
\item \strong{Profile}: Sampling
\item \strong{Facility-Site ID}: FL0037389 (this does not go in the Permit Number field)
\item \strong{Document Date}: From MM-DD-YYYY to MM-DD-YYYY (covering the desired monitoring year)
\item \strong{Document Type}: Discharge Monitoring Report (DMR)
}
\item Run the search and export the results to Excel (use the \strong{Export to Excel} button).
\item Save the exported \code{.xlsx} file and pass its path as \code{search_xlsx}.
}

The file must contain \code{HYPERLINK()} formulas in \strong{column A} pointing to
the individual DMR PDFs and document subject lines in \strong{column K}.  Both
are present in any standard OCULUS search export.  Also note that the search
may return other reports (e.g., toxicity results), which can be safely removed
from the Excel file.
}

\subsection{Document selection}{

The function keeps only monthly (\code{MO}) Part A documents for the requested
year.  Annual summary (\code{YR}) documents are excluded automatically.  If a
month has multiple submissions (e.g., a revision such as \code{"DMR (R) ..."}),
the most recently filed document is used.
}

\subsection{Reporting outfall}{

Kerry I&F reports flow separately for three internal monitoring sites
(\code{FLW-1}, \code{FLW-2}, and \code{FLW-3}).  \code{FLW-3} is the calculated combined
flow (\code{FLW-1} + \code{FLW-2}) and is
the value returned here, consistent with the outfall label used in prior
years' data for this facility.
}

\subsection{No Discharge (NOD) and Acceptable Non-Collection (ANC)}{

\code{NOD} (No Observable Discharge) is treated as zero flow.  \code{ANC} (Acceptable
Not Collected) is treated as \code{NA}.  Below-detection-limit values (e.g.
\code{"<2"}) are returned as the reported numeric detection limit (\code{2}), not
halved.
}

\subsection{Parameters not currently monitored}{

Total suspended solids (TSS) is not recorded at this facility and is
returned as \code{NA} for all months.
}

\subsection{Verifying results}{

On the initial run, supply a \code{pdf_dir} path so the downloaded PDFs are
retained for inspection.  Verify that the monitoring months, flow values,
and concentrations in the output data frame match those in the PDFs.
}
}
\examples{
\dontrun{
# Retrieve 2025 Kerry I&F DMR data
df <- util_ps_getkerry(
  yr          = 2025,
  search_xlsx = "Kerry_OCULUSSearchData_2025.xlsx"
)

# Keep PDFs and save results to Excel
df <- util_ps_getkerry(
  yr          = 2025,
  search_xlsx = "Kerry_OCULUSSearchData_2025.xlsx",
  pdf_dir     = "~/Desktop/Kerry_DMR_2025",
  out_file    = "~/Desktop/Kerry_DMR_2025_results.xlsx"
)
}
}
