% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_ps_getaoc.R
\name{util_ps_getaoc}
\alias{util_ps_getaoc}
\title{Get AOC LLC discharge monitoring report data from FDEP OCULUS}
\usage{
util_ps_getaoc(
  yr,
  search_xlsx,
  pdf_dir = NULL,
  out_file = NULL,
  keep_pdfs = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{yr}{numeric (length 1), the monitoring year to retrieve (e.g., 2025).}

\item{search_xlsx}{character, path to an OCULUS search-results spreadsheet
for facility FL0029653.  See Details for instructions on generating this
file.}

\item{pdf_dir}{character or NULL.  Directory in which to save the downloaded
PDFs.  Defaults to a temporary directory.  Ignored when
\code{keep_pdfs = FALSE} (the default).}

\item{out_file}{character or NULL.  If provided, the results data frame is
written to this path as an \code{.xlsx} workbook.}

\item{keep_pdfs}{logical.  If \code{FALSE} (default) the downloaded PDFs are
deleted after parsing.  Set to \code{TRUE} to retain them in \code{pdf_dir}.}

\item{quiet}{logical.  Suppress progress messages (default \code{FALSE}).}
}
\value{
A data frame with one row per available monitoring month, sorted by
month.  Calendar months for which no Part A document was found are omitted
(a message is printed when \code{quiet = FALSE}).  Columns:\tabular{lll}{
   Column \tab Type \tab Description \cr
   \code{yr} \tab integer \tab Monitoring year (from the PDF monitoring period). \cr
   \code{mo} \tab integer \tab Calendar month (1–12, from the PDF monitoring period). \cr
   \code{adf_mgd} \tab numeric \tab Average Daily Flow (MGD). \code{0} indicates no discharge (NOD). \cr
   \code{tn_mgl} \tab numeric \tab Total nitrogen grab-sample concentration (mg/L). \code{NA} when no discharge or not reported. \cr
}
}
\description{
Downloads and parses monthly Part A Discharge Monitoring Report (DMR) PDFs
for the AOC LLC industrial wastewater facility (NPDES permit FL0029653,
Polk County, FL) from the Florida Department of Environmental Protection
(FDEP) OCULUS public document management system.  Returns Average Daily Flow
and Total Nitrogen concentration for each available monitoring month.
}
\details{
\subsection{Generating the OCULUS search spreadsheet}{

The \code{search_xlsx} file is an Excel export from the FDEP OCULUS public
document portal.  To generate it:
\enumerate{
\item Navigate to \url{https://depedms.dep.state.fl.us} in a web browser.
\item Click \strong{Public Oculus Login} (no account required).
\item In the search form, set:
\itemize{
\item \strong{Catalog}: Wastewater
\item \strong{Profile}: Sampling
\item \strong{Facility-Site ID}: FL0029653 (this does not go in the Permit Number field)
\item \strong{Document Date}: From MM-DD-YYYY to MM-DD-YYYY (covering the desired monitoring year)
\item \strong{Document Type}: Discharge Monitoring Report (DMR)
}
\item Run the search and export the results to Excel (use the \strong{Export to Excel} button).
\item Save the exported \code{.xlsx} file and pass its path as \code{search_xlsx}.
}

The file must contain \code{HYPERLINK()} formulas in \strong{column A} pointing to
the individual DMR PDFs and document subject lines in \strong{column K}.  Both
are present in any standard OCULUS search export.
}

\subsection{Document selection}{

The function keeps only monthly (\code{MO}) Part A documents for the requested
year.  Annual summary (\code{YR}), Part B daily tables, and other document types
are excluded automatically.  If a month has multiple submissions (e.g., a
revision), the most recently filed document is used.
}

\subsection{Reporting period vs. OCULUS label}{

For some facilities the OCULUS cycle label (e.g., "JAN MO") may not align
with the calendar month of the monitoring period.  The month returned in the
output is always derived from the \strong{monitoring period dates inside the PDF},
not from the OCULUS label.
}

\subsection{No Discharge (NOD)}{

When the facility reports No Observable Discharge, \code{adf_mgd} is set to \code{0}
and \code{tn_mgl} is set to \code{NA}.  A zero flow value therefore implies no
discharge for that month.
}

\subsection{Parameters not currently monitored}{

Total phosphorus (TP), biochemical oxygen demand (BOD), and total suspended
solids (TSS) have not been recorded at this facility in recent years and are
not included in the output.
}
}
\examples{
\dontrun{
# Retrieve 2025 AOC DMR data
# (requires an OCULUS search spreadsheet generated as described in Details)
df <- util_ps_getaoc(
  yr          = 2025,
  search_xlsx = "AOC_OCULUSSearchData_2025.xlsx"
)

# Keep PDFs and save results to Excel
df <- util_ps_getaoc(
  yr          = 2025,
  search_xlsx = "AOC_OCULUSSearchData_2025.xlsx",
  pdf_dir     = "~/Desktop/AOC_DMR_2025",
  out_file    = "~/Desktop/AOC_DMR_2025_results.xlsx",
  keep_pdfs   = TRUE
)
}
}
