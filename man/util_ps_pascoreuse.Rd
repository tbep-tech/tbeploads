% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_ps_pascoreuse.R
\name{util_ps_pascoreuse}
\alias{util_ps_pascoreuse}
\title{Create Pasco Reuse point source input data}
\usage{
util_ps_pascoreuse(
  yr,
  res,
  golf = rep(0, length(yr)),
  ribs = rep(0, length(yr)),
  ag = rep(0, length(yr)),
  tn_conc = 9,
  n_coastal = 2
)
}
\arguments{
\item{yr}{integer vector of years}

\item{res}{numeric vector of residential reuse volumes (million gallons per year x 1000), one value per year}

\item{golf}{numeric vector of golf course reuse volumes (million gallons per year x 1000), one value per year}

\item{ribs}{numeric vector of rapid infiltration basin volumes (million gallons per year x 1000), one value per year}

\item{ag}{numeric vector of agricultural reuse volumes (million gallons per year x 1000), one value per year}

\item{tn_conc}{numeric, constant TN concentration in mg/L applied to all records (default \code{9})}

\item{n_coastal}{integer, number of coastal bay segment codes over which flow is divided equally (default \code{2})}
}
\value{
A \code{data.frame} with one row per year-month combination and columns:
\describe{
\item{Permit.Number}{character, \code{"PascoReuse"}}
\item{Facility.Name}{character, \code{"Pasco Reuse"}}
\item{Outfall.ID}{character, \code{"R-001"}}
\item{Year}{integer}
\item{Month}{integer, 1--12}
\item{Average.Daily.Flow..ADF...mgd.}{numeric, average daily flow in MGD}
\item{Total.N}{numeric, TN concentration in mg/L}
\item{TN.Unit}{character, \code{"mg/l"}}
\item{Total.P}{numeric, 0}
\item{TP.Unit}{character, \code{"mg/l"}}
\item{TSS}{numeric, 0}
\item{TSS.Unit}{character, \code{"mg/l"}}
\item{BOD}{numeric, 0}
\item{BOD.Unit}{character, \code{"mg/l"}}
}
}
\description{
Create Pasco Reuse point source input data from external hydrologic volume inputs and a constant TN concentration
}
\details{
Pasco County reuse hydrologic inputs are provided externally as annual volumes (in million gallons per year x 1000) broken into
land use categories: residential, golf courses, rapid infiltration basins (RIBs), and agriculture.
These are summed and converted to million gallons (MG) per year, then distributed evenly across
12 months and divided equally across \code{n_coastal} coastal bay segment codes to produce average
daily flow in MGD.  A constant TN concentration of \code{tn_conc} mg/L is assumed.  TP, TSS, and
BOD are set to zero.

The output format matches the standard point source input data frame used by \code{\link{anlz_dps_facility}}.
}
\examples{
util_ps_pascoreuse(
  yr   = 2022:2024,
  res  = c(744120, 522273, 344189),
  golf = c(0, 0, 0),
  ribs = c(0, 0, 0),
  ag   = c(169, 269, 153)
)
}
